steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --no-cache
      - -f
      - infra/docker/voice-agent.Dockerfile
      - -t
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
